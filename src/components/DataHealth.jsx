import React from 'react';
export default function DataHealth() {
  const items = [['ZIP Resolver','V1 Draft','Exact ZIP mapping, prefix fallback, and national fallback now exist.'],['Full US ZIP Coverage','Required','Production needs a full USPS/ZIP dataset mapped to clusters.'],['Internal Dispatch History','Ready','Use as carrier-pay truth layer. Requires outlier filtering.'],['Montway Benchmark','Ready','Use as competitor architecture/benchmark, not exact truth.'],['Vehicle Freight Tiers','V1 Ready','SUV progressive curve and freight-tier cliffs established.'],['Remote Friction','Needs Tuning','Cluster friction exists, but should be refined with historical data.'],['Outlier Handling','Required','Modified Mavericks and loaded Sprinters must be downweighted.'],['Auto Learning','Design Ready','Nightly dispatch updates should suggest changes, not blindly overwrite anchors.']];
  return <div className="healthGrid">{items.map(([title,status,detail]) => <div className="card" key={title}><div className="between"><h3>{title}</h3><span className={status.includes('Ready') ? 'pill green' : status === 'Required' ? 'pill red' : 'pill orange'}>{status}</span></div><p className="muted">{detail}</p></div>)}</div>;
}
